# Local-only project docs / notes — not part of the deployed site
CLAUDE.md
docs/
#json-edit/
# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Tailwind build tooling (compiled locally; the OUTPUT ot265/css/tailwind.css IS committed)
node_modules/
package-lock.json
